name: notepad
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  fluent_ui: ^4.7.3
  flutter:
    sdk: flutter
  flutter_acrylic: ^1.1.3
  system_theme: ^2.3.1
  window_manager: ^0.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
